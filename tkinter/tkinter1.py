from tkinter import *

root = Tk() # Tk() class creates our base window
theLabel = Label(root, text = "This is too easy!") # defines the label
theLabel.pack() # packs the label inside the window to display it
root.mainloop() # keeps the window on screen until closed